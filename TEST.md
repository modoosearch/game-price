# 대시보드 테스트

## 로컬에서 테스트

1. 백엔드 + 프론트 동시 실행:
   ```bash
   npm run dev
   ```
   - API: http://localhost:4000
   - 화면: http://localhost:3000

2. 브라우저에서 http://localhost:3000 접속 후 **지금 수집**으로 데이터 수집·갱신 확인.

## 화면 구성

- 헤더: 타이틀, 연결 상태, 광고문의, 지금 수집
- 좌측: 서버 목록 (현재가 대비 15시), 서버 선택
- 메인: 선택 서버 시세 차트, 시간/수량/체결가 테이블
- 하단: 전체게시판 / 서버별게시판 / 계산기 탭, 메시지 목록, nick / pw / massage + 등록

## 기존 레이아웃으로 전환

`client/src/App.jsx` 에서 `USE_DASHBOARD_VIEW = false` 로 변경하면 이전 게임머니 시세 중계 레이아웃이 표시됩니다.
